// This hook can be extended for reward-specific operations
// Currently, rewards are handled through events

export function useRewards() {
  // Placeholder for future reward-specific hooks
  return {};
}
